import { Editor } from "@/features/editor";

/**
 * Root component of the application.
 * Renders the full-screen editor as the sole view.
 *
 * @returns The rendered application tree.
 */
function App() {
  return <Editor />;
}

export default App;
