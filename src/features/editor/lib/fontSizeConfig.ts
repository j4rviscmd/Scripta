/** Default font size for the editor (in pixels). */
export const DEFAULT_FONT_SIZE = 16

/** Minimum allowed font size (in pixels). */
export const MIN_FONT_SIZE = 10

/** Maximum allowed font size (in pixels). */
export const MAX_FONT_SIZE = 32

/** Step size when incrementing / decrementing font size. */
export const FONT_SIZE_STEP = 1

/** Store key for persisting the editor font size. */
export const FONT_SIZE_STORE_KEY = 'editorFontSize'
