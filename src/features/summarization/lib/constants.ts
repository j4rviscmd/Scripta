/** Minimum character count to trigger summarization. */
export const MIN_CHARS = 100

/** Debounce delay for auto-summarization after content changes (ms). */
export const AUTO_SUMMARIZE_DEBOUNCE_MS = 10_000
